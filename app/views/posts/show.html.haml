%h1 Confession
%h2= @post.title
%p= @post.content

- if current_user && current_user.email == @post.user.email
	=link_to "Edit", edit_post_path(@post)
	=link_to "Let It Go?", post_path(@post), method: :delete

#comments
	%h2 Comments:
	=render @post.comments

	%h3 New Comment
	=render "comments/form"
